import './Header.css';

const Header = () => {
    return (
        <header className="App-header">
            <img src='./images/logo.png' alt="Header Logo"></img>
        </header>
    );
}

export default Header;
